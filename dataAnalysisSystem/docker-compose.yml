version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data_analysis.db:/app/data_analysis.db
    environment:
      - DATABASE_URL=sqlite:///./data_analysis.db
    restart: unless-stopped
