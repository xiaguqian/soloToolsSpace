server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xunreader?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 871502794
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

storage:
  type: local
  local:
    path: ./storage/chapters
  qiniu:
    access-key: your-access-key
    secret-key: your-secret-key
    bucket: your-bucket
    domain: your-qiniu-domain
  minio:
    endpoint: http://localhost:9000
    access-key: your-access-key
    secret-key: your-secret-key
    bucket: your-bucket

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html